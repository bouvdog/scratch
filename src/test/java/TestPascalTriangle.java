import leetcode.recursion.PTriSolution;

import static org.junit.jupiter.api.Assertions.assertEquals;
import org.junit.jupiter.api.Test;

public class TestPascalTriangle {

    /*
    @Test
    public void testRowIndices() {
        PTriSolution pt = new PTriSolution();
        int[] indices = pt.calculateRowIndices(5);
        assertEquals(4, indices[2]);
    }

    @Test
    public void testSetupTriangle() {
        PTriSolution pt = new PTriSolution();
        int numOfRows = 5;
        int[] triangle = new int[(5 * (5 + 1)) / 2];
        //pt.setupTriangle(triangle, pt.calculateRowIndices(numOfRows), numOfRows);
        assertEquals(1, triangle[11]);
    }

     */
}
