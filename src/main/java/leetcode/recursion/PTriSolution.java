package leetcode.recursion;

import java.util.List;

public class PTriSolution {
    /*
        public int[][] generate(final int numRows) {
            //int sizeOfTriangle = (numRows * (numRows + 1)) / 2;
            int[][] pt = new int[numRows][numRows];
            int lastRowIndex = numRows - 1;
            int index = 0;
            while (lastRowIndex - (lastRowIndex-index) >= 0) {
                pt[0][]
            }

            }


    public int[] setupTriangle(int[] triangle, int[] rowIndices, int numRows) {
        // setup Pascal's triangle
        for (int i = 0; i < numRows; i++) {
            triangle[rowIndices[i]] = 1;
        }
    }

*/

}
